delete from estados -- o espaço entre dele e from fica em branco, pois não escolhemos coluna, pois vamos deletar a linha inteira
where sigla = 'MN'                                                                                       

SELECT * FROM `estados`

delete from estados -- deletando a linha indentificada pelo id
WHERE id >= 1000

SELECT * FROM `estados`